import 'package:flutter/material.dart';

class ScreenplayScreen extends StatelessWidget {
  const ScreenplayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SizedBox.expand(),
    );
  }
}
